apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pr-preview-test-route
spec:
  hostnames:
    - prpt.byhe.me
  rules:
    - matches:
        - path:
            prefix: /
      filters:
        - type: RequestHeaderModifier
      forwardTo:
        - serviceName: pr-preview-test
          port: 80